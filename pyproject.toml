[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "smi-dnn"
version = "0.1.0"
description = "Reusable SMI-DNN experiment utilities and estimators."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["core", "estimators"]
